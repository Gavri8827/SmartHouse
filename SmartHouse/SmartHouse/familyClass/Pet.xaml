<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SmartHouse.familyClass.Pet">
    <ListView x:Name="PetList" SeparatorColor="Gray" ItemSelected="PetList_ItemSelected">
        <ListView.ItemTemplate>

            <DataTemplate>
                <ViewCell >
                    <StackLayout Orientation="Horizontal" Padding="10" Spacing="10" VerticalOptions="Fill" >
                        <Image Source="{Binding Image}"
                          HeightRequest="50"
                          WidthRequest="50" />
                        <StackLayout Orientation="Horizontal" Spacing="10">
                            <Label Text="{Binding Name}"
                              FontSize="Medium"
                              TextColor="Blue"
                              FontAttributes="Bold"/>
                            <Label Text="{Binding Kind}"
                                   FontSize="Small"
                                   TextColor="Blue" FontAttributes="Bold"/>
                        </StackLayout>

                        <StackLayout Orientation="Horizontal" HorizontalOptions="EndAndExpand" VerticalOptions="Center" >
                            <ImageButton Source="add.png" BackgroundColor ="Green" HeightRequest="50" WidthRequest="50" Clicked="ImageButton_Clicked"  />
                            <ImageButton Source="delete.png" BackgroundColor ="Green" HeightRequest="50" WidthRequest="50" Clicked="ImageButton_Clicked_1" />


                        </StackLayout>


                    </StackLayout>
                    
                    
                </ViewCell>
                
            </DataTemplate>
        </ListView.ItemTemplate>
        
    </ListView>
</ContentPage>